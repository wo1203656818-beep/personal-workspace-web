{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute personal-workspace-db --local --file=drizzle/0000_acoustic_typhoid_mary.sql && wrangler d1 execute personal-workspace-db --local --file=drizzle/0001_wakeful_doomsday.sql && wrangler d1 execute personal-workspace-db --local --file=drizzle/0002_clever_lizard.sql && wrangler d1 execute personal-workspace-db --local --file=drizzle/0004_cascade_delete.sql && wrangler d1 execute personal-workspace-db --local --file=drizzle/0003_indexes.sql",
    "db:migrate:remote": "wrangler d1 execute personal-workspace-db --remote --file=drizzle/0000_acoustic_typhoid_mary.sql && wrangler d1 execute personal-workspace-db --remote --file=drizzle/0001_wakeful_doomsday.sql && wrangler d1 execute personal-workspace-db --remote --file=drizzle/0002_clever_lizard.sql && wrangler d1 execute personal-workspace-db --remote --file=drizzle/0004_cascade_delete.sql && wrangler d1 execute personal-workspace-db --remote --file=drizzle/0003_indexes.sql",
    "hash-password": "node scripts/hash-password.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "format": "prettier --write src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@microsoft/microsoft-graph-client": "^3.0.7",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.31",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "@microsoft/microsoft-graph-types": "^2.43.1",
    "@types/node": "^26.1.1",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.113.0"
  }
}
